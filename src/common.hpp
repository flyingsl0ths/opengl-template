#pragma once

#include <cstdint>
#include <string_view>

using u8 [[maybe_unused]]  = std::uint8_t;
using u32 [[maybe_unused]] = std::uint32_t;
using s32 [[maybe_unused]] = std::int32_t;
using f32 [[maybe_unused]] = float;
using f64 [[maybe_unused]] = double;
using szt [[maybe_unused]] = std::size_t;
using str [[maybe_unused]] = std::string_view;
